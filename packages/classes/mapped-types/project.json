{
    "name": "classes-mapped-types",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/classes/mapped-types/src",
    "tags": [
        "classes"
    ],
    "targets": {
        "lint": {
            "executor": "@nx/eslint:lint"
        },
        "test": {
            "executor": "nx:run-commands",
            "options": {
                "command": "vitest run",
                "cwd": "packages/classes/mapped-types"
            }
        }
    }
}
