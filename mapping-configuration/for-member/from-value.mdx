---
title: fromValue
description: Assign a fixed value to one destination member.
---

```ts
forMember(
  (destination) => destination.status,
  fromValue('active'),
);
```

Objects are assigned as provided; `fromValue()` does not run them through a nested mapping.
